import styled from 'styled-components';

export const Form = styled.form`
    width: 80%;
    display: flex;
    flex-flow: row wrap;
    gap: 10px;

    font-family: "Poppins", Sans-serif;
    select{
        background: #fff;
        max-width: 250px;
    }
`;